// GenosDB is neither installed for runtime nor bundled: it loads from the
// jsDelivr CDN, always the latest release, where it resolves its own plugins
// beside itself. The `genosdb` devDependency exists only for its types.
export const GENOSDB = 'https://cdn.jsdelivr.net/npm/genosdb@latest/dist/index.min.js'
